services:
  app:
    build: .
    ports:
      - "8080:8080"
    volumes:
      - app-data:/app/data
    environment:
      - PORT=8080
      - LOG_FORMAT=json
      - LOG_LEVEL=info
      - ENVIRONMENT=production

volumes:
  app-data:
